## 📝 列表的基本概念

- 列表是 **有序的数据序列**，可以保存多个元素。

- 元素可以重复，也可以是不同类型（但通常建议保持同类元素，方便操作）。

- 定义方式：

  python

  ```
  items = [35, 12, 99, 68]
  items2 = list(range(1, 10))
  items3 = list("hello")
  ```

## ➕ 列表的运算

- **拼接**：`+`

  python

  ```
  [1, 2] + [3, 4]  # [1, 2, 3, 4]
  ```

- **重复**：`*`

  python

  ```
  [1, 2] * 3  # [1, 2, 1, 2, 1, 2]
  ```

- **成员运算**：`in / not in`

  python

  ```
  3 in [1, 2, 3]   # True
  "C++" not in ["Python", "Java"]  # True
  ```

## 🔢 索引与切片

- **索引**：

  - 正向：`[0]` 到 `[N-1]`
  - 反向：`[-1]` 到 `[-N]`

  python

  ```
  items = ["apple", "banana", "cherry"]
  items[0]   # "apple"
  items[-1]  # "cherry"
  ```

- **切片**：`[start:end:stride]`

  python

  ```
  items[1:3]     # 取索引 1 到 2
  items[::2]     # 隔一个取一个
  items[::-1]    # 倒序
  ```

- 切片也可用于 **修改**：

  python

  ```
  items[1:3] = ["x", "y"]
  ```

## ⚖️ 列表比较

- 可以用 `==, !=, <, >, <=, >=` 比较两个列表。
- 比较规则：逐个元素比较，直到能确定大小或相等。

## 🔄 遍历列表

- **方法一：索引遍历**

  python

  ```
  for i in range(len(items)):
      print(items[i])
  ```

- **方法二：直接遍历**

  python

  ```
  for item in items:
      print(item)
  ```

## 🎲 实例：掷骰子统计

- 用列表代替多个变量，简化统计逻辑：

  python

  ```
  import random
  counters = [0] * 6
  for _ in range(6000):
      face = random.randrange(1, 7)
      counters[face - 1] += 1
  for face in range(1, 7):
      print(f"{face}点出现了{counters[face-1]}次")
  ```

✅ **总结**： 列表是 Python 中最常用的容器型数据结构，支持 **存储多个元素、拼接/重复运算、索引/切片访问、比较、遍历** 等操作。它能让代码更简洁优雅，尤其适合批量数据处理。

-------

## 🔧 列表的常用方法

### 1. 添加与删除元素

- **append(x)**：在末尾追加元素
- **insert(i, x)**：在指定位置插入元素
- **remove(x)**：删除第一个匹配的元素（若不存在会报错）
- **pop([i])**：删除并返回指定位置的元素（默认最后一个）
- **clear()**：清空列表
- **del list[i]**：删除指定索引的元素（不返回值，性能略优于 `pop`）

### 2. 元素位置与频次

- **index(x[, start])**：返回元素第一次出现的索引位置
- **count(x)**：统计元素出现次数

### 3. 排序与反转

- **sort()**：原地排序（默认升序，可加 `reverse=True`）
- **reverse()**：原地反转

## 📝 列表生成式（List Comprehension）

- 更简洁优雅的创建方式，性能优于循环 + append

- 示例：

  python

  ```
  # 1 到 99 中能被 3 或 5 整除的数
  nums = [i for i in range(1, 100) if i % 3 == 0 or i % 5 == 0]
  
  # 平方列表
  nums2 = [x**2 for x in [35, 12, 97, 64, 55]]
  
  # 过滤大于 50 的数
  nums3 = [x for x in [35, 12, 97, 64, 55] if x > 50]
  ```

## 📐 嵌套列表

- 列表的元素可以是列表，用于表示矩阵或表格

- 示例：

  python

  ```
  scores = [[95, 83, 92], [80, 75, 82], [92, 97, 90]]
  print(scores[0])     # [95, 83, 92]
  print(scores[0][1])  # 83
  ```

## 🎲 应用示例

- **双色球选号程序**：利用 `random.sample` 和 `random.choice` 从列表中抽取元素
- **结合 rich 库**：可以美化输出，用表格和颜色展示结果

## ✅ 总结

- 列表是 **可变容器**，支持 **增删改查**、**排序反转**、**统计查询** 等操作
- **列表生成式** 是推荐的创建方式，简洁高效
- **嵌套列表** 可表示复杂结构，如矩阵、成绩表
- 在实际应用中，列表常用于 **数据收集、随机抽样、批量处理** 等场景



------

# 📝 Python 列表速查表（扩展版）

| 方法                  | 功能                     | 示例                     | 注意事项                                                     |
| --------------------- | ------------------------ | ------------------------ | ------------------------------------------------------------ |
| `append(x)`           | 在末尾添加元素           | `items.append(3)`        | 只能一次添加一个元素；如果要合并列表请用 `extend`            |
| `insert(i, x)`        | 在指定位置插入元素       | `items.insert(1, "SQL")` | 插入位置超出范围时，会自动插到末尾                           |
| `remove(x)`           | 删除第一个匹配的元素     | `items.remove("Java")`   | 若元素不存在会抛出 `ValueError`，建议先用 `if x in list` 判断 |
| `pop([i])`            | 删除并返回指定位置的元素 | `items.pop(1)`           | 索引超出范围会抛出 `IndexError`；默认删除最后一个            |
| `clear()`             | 清空列表                 | `items.clear()`          | 等价于 `del items[:]`                                        |
| `del list[i]`         | 删除指定索引的元素       | `del items[0]`           | 不返回值，性能略优于 `pop`                                   |
| `index(x[, start])`   | 返回元素第一次出现的索引 | `items.index("Python")`  | 若元素不存在会抛出 `ValueError`                              |
| `count(x)`            | 统计元素出现次数         | `items.count("Java")`    | 不存在时返回 `0`                                             |
| `sort(reverse=False)` | 排序（默认升序）         | `items.sort()`           | 会修改原列表；若要保留原列表请用 `sorted(items)`             |
| `reverse()`           | 反转列表                 | `items.reverse()`        | 仅反转，不排序                                               |

------

## 🔢 索引与切片

```python
nums = [10, 20, 30, 40, 50]

print(nums[0])     # 10
print(nums[-1])    # 50
print(nums[1:4])   # [20, 30, 40]
print(nums[::-1])  # [50, 40, 30, 20, 10]
```

------

## 📝 列表生成式（List Comprehension）

```python
# 1 到 99 中能被 3 或 5 整除的数
nums = [i for i in range(1, 100) if i % 3 == 0 or i % 5 == 0]

# 平方列表
squares = [x**2 for x in [35, 12, 97, 64, 55]]

# 过滤大于 50 的数
filtered = [x for x in [35, 12, 97, 64, 55] if x > 50]
```

⚠️ **注意**：列表生成式性能优于循环 + `append`，推荐使用。

------

## 📐 嵌套列表

```python
scores = [
    [95, 83, 92],
    [80, 75, 82],
    [92, 97, 90]
]

print(scores[0])     # [95, 83, 92]
print(scores[0][1])  # 83
```

⚠️ **注意**：嵌套列表常用于矩阵、表格；访问时需要多层索引。

------

## 🎲 应用示例：双色球选号

```python
import random

red_balls = list(range(1, 34))
blue_balls = list(range(1, 17))

selected_red = random.sample(red_balls, 6)  # 无放回抽样
selected_red.sort()
blue = random.choice(blue_balls)

print("红球:", selected_red, "蓝球:", blue)
```

------

## ✅ 总结

- 列表是 **可变容器**，支持 **增删改查、排序反转、统计查询**
- **remove / index** 在元素不存在时会报错，要小心
- **sort** 会修改原列表，若要保留原数据请用 `sorted`
- **列表生成式** 简洁高效，推荐优先使用
- **嵌套列表** 适合表示二维数据结构

------



# 🔑 `sorted()` 函数详解

## 📌 基本语法

```python
sorted(iterable, key=None, reverse=False)
```

- **iterable**：任何可迭代对象（列表、元组、字符串、集合、字典等）
- **key**：一个函数，用来提取排序依据（默认直接比较元素本身）
- **reverse**：布尔值，是否降序（默认 `False` 升序）

返回值：**一个新的已排序列表**（不会修改原对象）  [菜鸟教程](https://www.runoob.com/python/python-func-sorted.html)  [CSDN博客](https://blog.csdn.net/sangedianhao/article/details/137350769)。

------

## 🧩 基本用法

```python
nums = [5, 7, 6, 3, 4, 1, 2]
print(sorted(nums))        # [1, 2, 3, 4, 5, 6, 7]
print(nums)                # 原列表不变
```

------

## 🎯 常见场景

### 1. 对字符串排序

```python
print(sorted("python"))  
# ['h', 'n', 'o', 'p', 't', 'y']  按字符的 Unicode 编码排序
```

### 2. 对字典排序

- 默认对 **键** 排序：

```python
d = {"b": 2, "a": 1, "c": 3}
print(sorted(d))  
# ['a', 'b', 'c']
```

- 按值排序：

```python
print(sorted(d.items(), key=lambda x: x[1]))
# [('a', 1), ('b', 2), ('c', 3)]
```

### 3. 使用 `key` 参数

- 按绝对值排序：

```python
nums = [36, 5, -12, 9, -21]
print(sorted(nums, key=abs))
# [5, 9, -12, -21, 36]
```

- 忽略大小写排序：

```python
words = ["bob", "about", "Zoo", "Credit"]
print(sorted(words, key=str.lower))
# ['about', 'bob', 'Credit', 'Zoo']
```

### 4. 降序排序

```python
nums = [3, 1, 4, 1, 5, 9]
print(sorted(nums, reverse=True))
# [9, 5, 4, 3, 1, 1]
```

### 5. 复杂对象排序

```python
students = [
    ("john", "A", 15),
    ("jane", "B", 12),
    ("dave", "B", 10)
]
# 按年龄排序
print(sorted(students, key=lambda s: s[2]))
# [('dave', 'B', 10), ('jane', 'B', 12), ('john', 'A', 15)]
```

------

## ⚡ `sorted()` vs `.sort()`

| 特性     | `sorted()`     | `.sort()`             |
| -------- | -------------- | --------------------- |
| 作用对象 | 任意可迭代对象 | 仅限列表              |
| 返回值   | 新的已排序列表 | 原地修改，返回 `None` |
| 灵活性   | 更高           | 较低                  |

------

## 📝 总结

- `sorted()` 是 **函数**，返回新列表；`.sort()` 是 **列表方法**，原地修改。
- `key` 参数是核心，用于自定义排序规则。
- `reverse=True` 可快速实现降序。

------

