# 📝 Python 集合操作速查表

## 🔹 创建集合

- **字面量**：`s = {1, 2, 3}`
- **构造器**：`s = set([1, 2, 2, 3]) # {1, 2, 3}`
- **空集合**：`s = set()`（注意 `{}` 是空字典）
- **生成式**：`s = {x for x in range(10) if x % 2 == 0}`

------

## 🔹 基本性质

- **无序性**：元素没有顺序，不能用索引访问
- **互异性**：元素唯一，不允许重复
- **成员测试**：`x in s` / `x not in s`

------

## 🔹 集合运算

| 运算   | 符号写法 | 方法写法                    | 示例                   |      |
| ------ | -------- | --------------------------- | ---------------------- | ---- |
| 交集   | `A & B`  | `A.intersection(B)`         | `{2, 4, 6}`            |      |
| 并集   | `A 丨B`  | `A.union(B)`                | `{1,2,3,4,5,6,7,8,10}` |      |
| 差集   | `A - B`  | `A.difference(B)`           | `{1,3,5,7}`            |      |
| 对称差 | `A ^ B`  | `A.symmetric_difference(B)` | `{1,3,5,7,8,10}`       |      |

------

## 🔹 集合关系

| 判断   | 符号     | 方法              | 示例       |
| ------ | -------- | ----------------- | ---------- |
| 子集   | `A <= B` | `A.issubset(B)`   | True       |
| 真子集 | `A < B`  | -                 | True       |
| 超集   | `A >= B` | `A.issuperset(B)` | True       |
| 不相交 | -        | `A.isdisjoint(B)` | True/False |

------

## 🔹 修改集合

| 操作             | 方法                               | 示例            |      |
| ---------------- | ---------------------------------- | --------------- | ---- |
| 添加元素         | `s.add(x)`                         | `s.add(10)`     |      |
| 删除元素（安全） | `s.discard(x)`                     | 不报错          |      |
| 删除元素（严格） | `s.remove(x)`                      | 不存在时报错    |      |
| 随机删除并返回   | `s.pop()`                          | 返回被删元素    |      |
| 清空集合         | `s.clear()`                        | `set()`         |      |
| 更新并集         | `s.update(t)`                      | 等价于 `s = t`  |      |
| 更新交集         | `s.intersection_update(t)`         | 等价于 `s &= t` |      |
| 更新差集         | `s.difference_update(t)`           | 等价于 `s -= t` |      |
| 更新对称差       | `s.symmetric_difference_update(t)` | 等价于 `s ^= t` |      |

------

## 🔹 不可变集合

- **定义**：`fs = frozenset([1, 2, 3])`
- **特点**：不可修改，但可作为字典键或集合元素
- **支持运算**：交集、并集、差集、对称差等

------

## 🔹 常用技巧

- **去重**：`list(set([1,2,2,3])) # [1,2,3]`
- **集合推导式**：`{x**2 for x in range(5)}`
- **快速判重**：`len(s) != len(set(s))`

------

