# 🐍 Python 函数和模块用法总结

## 1. 函数的基本概念

- 定义函数

  ：使用 

  ```
  def
  ```

   关键字

  ```python
  def func_name(params):
      # 函数体
      return result
  ```

- **参数**：函数的输入（自变量）

- **返回值**：函数的输出（因变量），通过 `return` 返回

- **无返回值**：若没有 `return`，默认返回 `None`

------

## 2. 函数的参数类型

- 位置参数

  ：按顺序传入

  ```python
  def add(a, b): return a + b
  add(1, 2)  # 3
  ```

- 关键字参数

  ：通过 

  ```
  参数名=值
  ```

   传入

  ```python
  add(b=2, a=1)
  ```

- 强制位置参数

  （Python 3.8+）：在参数列表中用 

  ```
  /
  ```

  ```python
  def f(a, b, /): ...
  ```

- 命名关键字参数

  ：在参数列表中用 

  ```
  *
  ```

  ```python
  def f(*, a, b): ...
  ```

- 默认参数

  ：给参数设定默认值

  ```python
  def roll_dice(n=2): ...
  ```

- 可变位置参数

  ：

  ```
  *args
  ```

   → 组装成元组

  ```python
  def add(*args): return sum(args)
  ```

- 可变关键字参数

  ：

  ```
  **kwargs
  ```

   → 组装成字典

  ```python
  def foo(**kwargs): print(kwargs)
  ```

------

## 3. 模块的使用

- **模块概念**：每个 `.py` 文件就是一个模块

- 导入模块

  ：

  - `import module` → 使用 `module.func()` 调用
  - `import module as m` → 使用别名
  - `from module import func` → 直接导入函数
  - `from module import func as f` → 给函数起别名

- **命名冲突解决**：通过模块名或别名区分同名函数

------

## 4. 标准库与内置函数

- 标准库模块

  ：如 

  ```
  math
  ```

  、

  ```
  random
  ```

  、

  ```
  time
  ```

  ，提供大量常用函数

  ```python
  import math
  math.factorial(5)  # 120
  ```

  

  Python 标准库中提供了大量的模块和函数来简化我们的开发工作，我们之前用过的`random`模块就为我们提供了生成随机数和进行随机抽样的函数；而`time`模块则提供了和时间操作相关的函数；我们之前用到过的`math`模块中还包括了计算正弦、余弦、指数、对数等一系列的数学函数。随着我们深入学习 Python 语言，我们还会用到更多的模块和函数。

  Python 标准库中还有一类函数是不需要`import`就能够直接使用的，我们将其称之为**内置函数**，这些内置函数不仅有用而且还很常用，下面的表格列出了一部分的内置函数。

  | 函数    | 说明                                                         |
  | ------- | ------------------------------------------------------------ |
  | `abs`   | 返回一个数的绝对值，例如：`abs(-1.3)`会返回`1.3`。           |
  | `bin`   | 把一个整数转换成以`'0b'`开头的二进制字符串，例如：`bin(123)`会返回`'0b1111011'`。 |
  | `chr`   | 将Unicode编码转换成对应的字符，例如：`chr(8364)`会返回`'€'`。 |
  | `hex`   | 将一个整数转换成以`'0x'`开头的十六进制字符串，例如：`hex(123)`会返回`'0x7b'`。 |
  | `input` | 从输入中读取一行，返回读到的字符串。                         |
  | `len`   | 获取字符串、列表等的长度。                                   |
  | `max`   | 返回多个参数或一个可迭代对象中的最大值，例如：`max(12, 95, 37)`会返回`95`。 |
  | `min`   | 返回多个参数或一个可迭代对象中的最小值，例如：`min(12, 95, 37)`会返回`12`。 |
  | `oct`   | 把一个整数转换成以`'0o'`开头的八进制字符串，例如：`oct(123)`会返回`'0o173'`。 |
  | `open`  | 打开一个文件并返回文件对象。                                 |
  | `ord`   | 将字符转换成对应的Unicode编码，例如：`ord('€')`会返回`8364`。 |
  | `pow`   | 求幂运算，例如：`pow(2, 3)`会返回`8`；`pow(2, 0.5)`会返回`1.4142135623730951`。 |
  | `print` | 打印输出。                                                   |
  | `range` | 构造一个范围序列，例如：`range(100)`会产生`0`到`99`的整数序列。 |
  | `round` | 按照指定的精度对数值进行四舍五入，例如：`round(1.23456, 4)`会返回`1.2346`。 |
  | `sum`   | 对一个序列中的项从左到右进行求和运算，例如：`sum(range(1, 101))`会返回`5050`。 |
  | `type`  | 返回对象的类型，例如：`type(10)`会返回`int`；而` type('hello')`会返回`str`。 |

------

## 5. 总结

- **函数**：封装可复用的逻辑，避免重复代码
- **模块**：管理和组织函数，避免命名冲突
- **标准库 & 内置函数**：用好现成工具，少造轮子

------

# 🐍 Python 函数 & 模块速查表

## 🔹 函数定义与调用

| 语法/概念  | 示例                         | 说明                        |
| ---------- | ---------------------------- | --------------------------- |
| 定义函数   | `def func(a, b): return a+b` | 使用 `def` 关键字           |
| 调用函数   | `func(2, 3)`                 | 在函数名后加括号传参        |
| 返回值     | `return result`              | 没有 `return` → 默认 `None` |
| 无参数函数 | `def hello(): print("hi")`   | 圆括号必须保留              |

------

## 🔹 参数类型

| 类型           | 示例                   | 说明                       |
| -------------- | ---------------------- | -------------------------- |
| 位置参数       | `add(1, 2)`            | 按顺序传入                 |
| 关键字参数     | `add(b=2, a=1)`        | 指定参数名                 |
| 强制位置参数   | `def f(a, b, /): ...`  | `/` 前的参数只能按位置传   |
| 命名关键字参数 | `def f(*, a, b): ...`  | `*` 后的参数必须用关键字传 |
| 默认参数       | `def roll(n=2): ...`   | 不传参时使用默认值         |
| 可变位置参数   | `def f(*args): ...`    | 参数打包成元组             |
| 可变关键字参数 | `def f(**kwargs): ...` | 参数打包成字典             |

------

## 🔹 模块导入方式

| 语法     | 示例                         | 说明                |
| -------- | ---------------------------- | ------------------- |
| 导入模块 | `import math`                | 使用 `math.sqrt(9)` |
| 模块别名 | `import math as m`           | 使用 `m.sqrt(9)`    |
| 导入函数 | `from math import sqrt`      | 直接用 `sqrt(9)`    |
| 函数别名 | `from math import sqrt as s` | 使用 `s(9)`         |

------

## 🔹 命名冲突解决

| 场景               | 示例                                                     | 说明                   |
| ------------------ | -------------------------------------------------------- | ---------------------- |
| 同名函数在不同模块 | `module1.foo()` / `module2.foo()`                        | 使用 **模块名.函数名** |
| 导入同名函数       | `from m1 import foo as f1`<br>`from m2 import foo as f2` | 使用别名区分           |

------

## 🔹 常用标准库

| 模块     | 常用函数                          |
| -------- | --------------------------------- |
| `math`   | `sqrt`, `factorial`, `sin`, `log` |
| `random` | `randint`, `choice`, `shuffle`    |
| `time`   | `time`, `sleep`, `strftime`       |

------

## 🔹 常用内置函数（无需导入）

| 函数                | 说明           |
| ------------------- | -------------- |
| `abs(x)`            | 绝对值         |
| `len(s)`            | 长度           |
| `max()/min()`       | 最大/最小值    |
| `sum(iterable)`     | 求和           |
| `range(n)`          | 生成序列       |
| `type(obj)`         | 查看类型       |
| `print()`           | 输出           |
| `input()`           | 输入           |
| `bin()/oct()/hex()` | 进制转换       |
| `ord()/chr()`       | 字符 ↔ Unicode |
| `pow(x, y)`         | 幂运算         |

------

✅ **总结**：

- **函数** → 封装可复用逻辑，减少重复代码
- **模块** → 组织和管理函数，避免命名冲突
- **标准库 & 内置函数** → 善用现成工具，提高效率